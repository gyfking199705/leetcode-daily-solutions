# LeetCode 208. 实现 Trie（前缀树）精讲

**日期**: 2026-03-15  
**难度**: Medium  
**标签**: 设计、字典树、字符串、哈希表

---

## 一、题目描述

Trie（发音类似 "try"），或称 **前缀树** 或 **字典树**，是一种树形数据结构，用于高效地存储和检索字符串数据集中的键。

### 题目要求

实现 `Trie` 类：

- `Trie()` 初始化前缀树对象。
- `void insert(String word)` 向前缀树中插入字符串 `word`。
- `boolean search(String word)` 如果字符串 `word` 在前缀树中，返回 `true`（即，在检索之前已经插入）；否则，返回 `false`。
- `boolean startsWith(String prefix)` 如果之前已经插入的字符串 `word` 的前缀之一为 `prefix`，返回 `true`；否则，返回 `false`。

### 示例

```
输入
["Trie", "insert", "search", "search", "startsWith", "insert", "search"]
[[], ["apple"], ["apple"], ["app"], ["app"], ["app"], ["app"]]

输出
[null, null, true, false, true, null, true]

解释
Trie trie = new Trie();
trie.insert("apple");
trie.search("apple");   // 返回 True
trie.search("app");     // 返回 False
trie.startsWith("app"); // 返回 True
trie.insert("app");
trie.search("app");     // 返回 True
```

---

## 二、核心思路

### 2.1 什么是Trie树？

Trie树的核心思想是：**用空间换时间**，通过复用公共前缀来加速字符串的查询。

```
插入: apple, app, banana

           root
          /    \
         a      b
        /        \
       p          a
      / \          \
     p   #         n
    /               \
   l                 a
  /                   \
 e                     n
/                       \
e                         a
#                           #

# 表示 isEnd = true (单词结束)
```

### 2.2 Trie节点结构

每个节点包含两个关键属性：

| 属性 | 类型 | 说明 |
|------|------|------|
| `children` | 哈希表/数组 | 存储子节点，键是字符，值是对应的子节点 |
| `isEnd` | 布尔值 | 标记该节点是否为某个完整单词的结尾 |

---

## 三、代码实现

### 3.1 Python 实现（推荐面试使用）

```python
class TrieNode:
    """Trie树节点"""
    def __init__(self):
        self.children = {}  # 子节点字典
        self.is_end = False  # 是否为单词结尾


class Trie:
    def __init__(self):
        """初始化前缀树对象"""
        self.root = TrieNode()

    def insert(self, word: str) -> None:
        """
        向前缀树中插入字符串 word
        时间复杂度: O(m)，m为word长度
        """
        node = self.root
        for char in word:
            # 如果字符不存在，创建新节点
            if char not in node.children:
                node.children[char] = TrieNode()
            # 移动到子节点
            node = node.children[char]
        # 标记单词结束
        node.is_end = True

    def search(self, word: str) -> bool:
        """
        检索字符串 word 是否完整存在于前缀树中
        时间复杂度: O(m)，m为word长度
        """
        node = self._search_prefix(word)
        # 节点存在且是单词结尾才算匹配成功
        return node is not None and node.is_end

    def startsWith(self, prefix: str) -> bool:
        """
        检索是否有单词以 prefix 为前缀
        时间复杂度: O(m)，m为prefix长度
        """
        return self._search_prefix(prefix) is not None

    def _search_prefix(self, prefix: str) -> TrieNode:
        """
        辅助方法：查找前缀对应的最后一个节点
        返回: 如果找到返回对应节点，否则返回 None
        """
        node = self.root
        for char in prefix:
            if char not in node.children:
                return None
            node = node.children[char]
        return node


# 使用示例
if __name__ == "__main__":
    trie = Trie()
    trie.insert("apple")
    print(trie.search("apple"))    # True
    print(trie.search("app"))      # False
    print(trie.startsWith("app"))  # True
    trie.insert("app")
    print(trie.search("app"))      # True
```

### 3.2 Java 实现

```java
class TrieNode {
    // 子节点数组，假设只包含小写字母a-z
    TrieNode[] children;
    // 是否为单词结尾
    boolean isEnd;
    
    public TrieNode() {
        children = new TrieNode[26];
        isEnd = false;
    }
}

class Trie {
    private TrieNode root;

    public Trie() {
        root = new TrieNode();
    }
    
    public void insert(String word) {
        TrieNode node = root;
        for (char c : word.toCharArray()) {
            int index = c - 'a';
            if (node.children[index] == null) {
                node.children[index] = new TrieNode();
            }
            node = node.children[index];
        }
        node.isEnd = true;
    }
    
    public boolean search(String word) {
        TrieNode node = searchPrefix(word);
        return node != null && node.isEnd;
    }
    
    public boolean startsWith(String prefix) {
        return searchPrefix(prefix) != null;
    }
    
    private TrieNode searchPrefix(String prefix) {
        TrieNode node = root;
        for (char c : prefix.toCharArray()) {
            int index = c - 'a';
            if (node.children[index] == null) {
                return null;
            }
            node = node.children[index];
        }
        return node;
    }
}
```

### 3.3 C++ 实现

```cpp
struct TrieNode {
    unordered_map<char, TrieNode*> children;
    bool isEnd;
    
    TrieNode() : isEnd(false) {}
};

class Trie {
private:
    TrieNode* root;
    
    TrieNode* searchPrefix(string prefix) {
        TrieNode* node = root;
        for (char c : prefix) {
            if (node->children.find(c) == node->children.end()) {
                return nullptr;
            }
            node = node->children[c];
        }
        return node;
    }
    
public:
    Trie() {
        root = new TrieNode();
    }
    
    void insert(string word) {
        TrieNode* node = root;
        for (char c : word) {
            if (node->children.find(c) == node->children.end()) {
                node->children[c] = new TrieNode();
            }
            node = node->children[c];
        }
        node->isEnd = true;
    }
    
    bool search(string word) {
        TrieNode* node = searchPrefix(word);
        return node != nullptr && node->isEnd;
    }
    
    bool startsWith(string prefix) {
        return searchPrefix(prefix) != nullptr;
    }
};
```

---

## 四、复杂度分析

| 操作 | 时间复杂度 | 空间复杂度 | 说明 |
|------|-----------|-----------|------|
| `insert` | O(m) | O(m) | m为字符串长度 |
| `search` | O(m) | O(1) | m为字符串长度 |
| `startsWith` | O(m) | O(1) | m为前缀长度 |

### 对比哈希表

| 数据结构 | 前缀查询 | 最长公共前缀 | 字典序遍历 |
|----------|----------|--------------|------------|
| Trie | O(m) | O(m) | O(N) |
| HashSet | O(N×m) | O(N×m) | 需排序O(NlogN) |

---

## 五、关键点总结

### 5.1 面试常考问题

1. **为什么需要 `isEnd` 标记？**
   - 区分"前缀"和"完整单词"。例如插入"apple"后，"app"是前缀但不是完整单词，search("app")应返回false。

2. **如何处理重复插入？**
   - 无需特殊处理，重复插入只是再次将 `isEnd` 设为 true。

3. **字符集很大怎么办？**
   - 使用哈希表存储 children 代替数组，只存储实际出现的字符。

### 5.2 常见变体

```python
# 1. 带计数的前缀树（可用于求前缀出现次数）
class TrieNodeWithCount:
    def __init__(self):
        self.children = {}
        self.count = 0  # 经过该节点的单词数
        self.is_end = False

# 2. 带单词列表的前缀树（可用于自动补全）
class TrieNodeWithWords:
    def __init__(self):
        self.children = {}
        self.words = []  # 以当前节点为前缀的所有单词
        self.is_end = False
```

### 5.3 易错点

- ❌ 忘记在 insert 结束时标记 `is_end = True`
- ❌ search 只查节点存在性，没查 `is_end`
- ❌ 数组实现时字符映射出错（如大写字母、数字等）

---

## 六、相关题目

| 题号 | 题目 | 难度 | 关联 |
|------|------|------|------|
| 211 | 添加与搜索单词 - 数据结构设计 | Medium | Trie + DFS |
| 212 | 单词搜索 II | Hard | Trie + DFS |
| 648 | 单词替换 | Medium | Trie前缀匹配 |
| 677 | 键值映射 | Medium | Trie前缀求和 |
| 720 | 词典中最长的单词 | Easy | Trie完整匹配 |
| 745 | 前缀和后缀搜索 | Hard | Trie变体 |

---

## 七、应用场景

1. **自动补全** - 搜索引擎、IDE代码提示
2. **拼写检查** - 快速判断单词是否存在
3. **IP路由** - 最长前缀匹配
4. **单词游戏** - Boggle/Scrabble等
5. **DNA序列分析** - 生物信息学

---

## 八、参考资料

- [LeetCode官方题解](https://leetcode.cn/problems/implement-trie-prefix-tree/)
- [Trie树详解 - 掘金](https://juejin.cn/post/7613605454283554867)
- [Trie树实现 - CSDN](https://blog.csdn.net/u012723673/article/details/157769612)

---

*Generated by Kimi Claw - LeetCode Daily Problem Series*
