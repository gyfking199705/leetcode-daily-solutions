# LeetCode 102 - 二叉树的层序遍历

**日期**: 2026-03-15  
**难度**: 中等  
**标签**: 树、BFS、广度优先搜索、二叉树

---

## 📋 题目描述

给你二叉树的根节点 `root`，返回其节点值的**层序遍历**。（即逐层地，从左到右访问所有节点）。

### 示例 1：
```
    3
   / \
  9  20
    /  \
   15   7
```
**输入**: `root = [3,9,20,null,null,15,7]`  
**输出**: `[[3],[9,20],[15,7]]`

### 示例 2：
**输入**: `root = [1]`  
**输出**: `[[1]]`

### 示例 3：
**输入**: `root = []`  
**输出**: `[]`

### 约束条件：
- 树中节点数目范围在 `[0, 2000]`
- `-1000 <= Node.val <= 1000`

---

## 💡 解题思路

### 核心思想：广度优先搜索 (BFS)

层序遍历的本质就是**按层次遍历**，这天然适合用**队列（Queue）**来实现：

1. **初始化**：将根节点加入队列
2. **逐层处理**：
   - 记录当前层的节点数量 `level_size`
   - 依次取出 `level_size` 个节点，记录它们的值
   - 将这些节点的左右子节点加入队列
3. **重复**直到队列为空

### 为什么用队列？

队列的**先进先出（FIFO）**特性完美匹配层序遍历的需求：
- 先访问的节点，其子节点也先被加入队列
- 同层的节点会连续处理

### 关键技巧：如何区分不同层？

在处理每一层之前，先记录当前队列的长度 `n`，这正好就是当前层的节点数！
```python
level_size = len(queue)  # 当前层的节点数
for _ in range(level_size):
    node = queue.popleft()
    # 处理节点...
```

---

## 📝 代码实现

### Python 实现

```python
from collections import deque
from typing import List, Optional

# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        if not root:
            return []
        
        result = []
        queue = deque([root])
        
        while queue:
            # 关键：记录当前层的节点数量
            level_size = len(queue)
            current_level = []
            
            # 只处理当前层的节点
            for _ in range(level_size):
                node = queue.popleft()
                current_level.append(node.val)
                
                # 将子节点加入队列，准备下一层处理
                if node.left:
                    queue.append(node.left)
                if node.right:
                    queue.append(node.right)
            
            result.append(current_level)
        
        return result
```

### Java 实现

```java
import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;
import java.util.Queue;

class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;
    TreeNode() {}
    TreeNode(int val) { this.val = val; }
    TreeNode(int val, TreeNode left, TreeNode right) {
        this.val = val;
        this.left = left;
        this.right = right;
    }
}

class Solution {
    public List<List<Integer>> levelOrder(TreeNode root) {
        List<List<Integer>> result = new ArrayList<>();
        
        if (root == null) {
            return result;
        }
        
        Queue<TreeNode> queue = new LinkedList<>();
        queue.offer(root);
        
        while (!queue.isEmpty()) {
            int levelSize = queue.size();
            List<Integer> currentLevel = new ArrayList<>();
            
            for (int i = 0; i < levelSize; i++) {
                TreeNode node = queue.poll();
                currentLevel.add(node.val);
                
                if (node.left != null) {
                    queue.offer(node.left);
                }
                if (node.right != null) {
                    queue.offer(node.right);
                }
            }
            
            result.add(currentLevel);
        }
        
        return result;
    }
}
```

### C++ 实现

```cpp
#include <vector>
#include <queue>

struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
    TreeNode() : val(0), left(nullptr), right(nullptr) {}
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
    TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
};

class Solution {
public:
    std::vector<std::vector<int>> levelOrder(TreeNode* root) {
        std::vector<std::vector<int>> result;
        
        if (!root) {
            return result;
        }
        
        std::queue<TreeNode*> queue;
        queue.push(root);
        
        while (!queue.empty()) {
            int levelSize = queue.size();
            std::vector<int> currentLevel;
            
            for (int i = 0; i < levelSize; i++) {
                TreeNode* node = queue.front();
                queue.pop();
                currentLevel.push_back(node->val);
                
                if (node->left) {
                    queue.push(node->left);
                }
                if (node->right) {
                    queue.push(node->right);
                }
            }
            
            result.push_back(currentLevel);
        }
        
        return result;
    }
};
```

---

## 📊 复杂度分析

### 时间复杂度：O(N)
- 每个节点恰好被访问一次
- N 为二叉树的节点总数

### 空间复杂度：O(W)
- W 是二叉树的最大宽度
- 队列中最多同时存储一层的节点数
- 最坏情况下（完全二叉树的最底层），空间复杂度为 O(N/2) = O(N)

---

## 🎯 关键点总结

1. **队列是核心**：BFS天然适合用队列实现

2. **分层技巧**：
   - 每层开始前记录 `level_size = queue.size()`
   - 只循环 `level_size` 次，确保处理完当前层

3. **边界处理**：
   - 空树直接返回 `[]`
   - 注意检查子节点是否存在再加入队列

4. **扩展方向**：
   - 本题是BFS的基础模板
   - 可以延伸到：Z字形遍历、按层求平均值、找最深层最左/右节点等

---

## 🔗 类似题目

| 题号 | 题目 | 难度 | 关联点 |
|------|------|------|--------|
| 107 | 二叉树的层序遍历 II | 中等 | 自底向上输出，结果reverse即可 |
| 103 | 二叉树的锯齿形层序遍历 | 中等 | 奇偶层反向，可用双端队列或reverse |
| 199 | 二叉树的右视图 | 中等 | 每层只取最后一个节点 |
| 637 | 二叉树的层平均值 | 简单 | 每层求平均值 |
| 111 | 二叉树的最小深度 | 简单 | BFS找最短路径 |
| 116/117 | 填充每个节点的下一个右侧节点指针 | 中等 | 层序遍历，连接同层节点 |
| 515 | 在每个树行中找最大值 | 中等 | 每层找最大值 |
| 513 | 找树左下角的值 | 中等 | 最深层最左节点 |

---

## 🧠 面试常见问题

**Q: BFS和DFS都能做吗？**  
A: 层序遍历就是BFS的经典应用，虽然DFS也能做（记录深度），但BFS更直观自然。

**Q: 可以用递归实现吗？**  
A: 可以，但需要传递当前层数信息，不如迭代直观：
```python
def dfs(node, depth):
    if not node: return
    if depth == len(result):
        result.append([])
    result[depth].append(node.val)
    dfs(node.left, depth + 1)
    dfs(node.right, depth + 1)
```

**Q: 如果要求Z字形遍历怎么做？**  
A: 两种方法：
1. 正常层序遍历，奇数层reverse结果
2. 使用双端队列，奇数层从头部插入

---

*记住：层序遍历 = 队列 + 分层处理，这是BFS在树问题中的黄金模板！*
